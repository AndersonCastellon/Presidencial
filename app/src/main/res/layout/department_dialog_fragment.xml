<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/activity_horizontal_margin">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/activity_horizontal_margin"
        android:text="@string/selectDepartment"
        android:textAlignment="center"
        android:textStyle="bold" />

    <include layout="@layout/spinner_department" />

    <com.dd.processbutton.iml.ActionProcessButton
        android:id="@+id/actionButtonSetDepartment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:layout_marginTop="@dimen/activity_horizontal_margin"
        android:text="@string/Continue"
        android:textColor="@color/icons"
        android:textSize="@dimen/textSizeButton"
        app:pb_colorComplete="@color/green_complete"
        app:pb_colorError="@color/red_error"
        app:pb_colorNormal="@color/primary"
        app:pb_colorPressed="@color/primary_dark"
        app:pb_colorProgress="@color/purple_progress"
        app:pb_textComplete="@string/textCompleteActionButtons"
        app:pb_textError="@string/textErrorActionButtos"
        app:pb_textProgress="@string/textProgressActionButtons" />

</LinearLayout>